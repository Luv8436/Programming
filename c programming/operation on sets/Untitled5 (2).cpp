//program for finding powerset of two sets
#include<stdio.h>
int main()
{
	int num,i;// number of elements in set.
	printf("enter the number of elements in set\n");
	scanf("%d",&num);
	int set[num];
	printf("enter the elements of set ");
	for(i=0;i<num1;i++){//take elements of set
	scanf("%d",&set[i]);
	}
	
	
}
