#include<stdio.h>
int main()
{
    // enter the length of arr;
    printf("enter the length of arr : ");
    int len;
    scanf("%d",&len);
    int arr[len];
    // enter the elements into the array
    for(int i=0;i<len;i++)
    {
        scanf
    }
}